# Chay Runtime Project Rules

When this repository contains `chay-memory/` or `.chay/`, read `chay-memory/rules/chay-runtime.md` before coding.

Do not edit outside `selected_files`, `allowed_files`, or graph `code_targets`. If selected files do not match the user's goal, stop and ask for explicit `cr go "Task" --files path/to/file`.
