# Chay Runtime Steering

When `chay-memory/` or `.chay/` exists, read `chay-memory/rules/chay-runtime.md` before source files.

Follow the handoff, `chay-structure/features/<feature_id>.md`, folder structure, API graph, PlantUML files, and graph code targets. Stop if scope is unclear or mismatched.
